pub trait StringManipulation {
    fn hamming_distance(&self, string: &Vec<u8>) -> usize;
}

impl StringManipulation for String {
  pub fn hamming_distance(&self, string: &Vec<v8>) -> usize {
    0
  }

}